import styled from "styled-components";

export const Input = styled.input.attrs(
    {
        type: 'number',
        placeholder: 'Some text',
        className:'border h-fit pl-1 pr-20 py-3 w-fit mt-2'
    })``